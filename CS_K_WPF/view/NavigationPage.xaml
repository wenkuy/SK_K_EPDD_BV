<Page
    x:Class="CS_K_WPF.NavigationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CS_K_WPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="NavigationPage"
    d:DesignHeight="450"
    d:DesignWidth="800"
    DataContext="{Binding Source={StaticResource ModelLocator}, Path=LocNavigationPageVM}"
    mc:Ignorable="d">

    <Grid>
        <Border Background="LightGray">
            <StackPanel VerticalAlignment="Center">
                <Button
                    Height="80"
                    Command="{Binding HomePageCommand}"
                    Content="首页"
                    FontSize="30" />
                <Button
                    Height="80"
                    Command="{Binding MonitorPageCommand}"
                    Content="实时监控"
                    FontSize="30" />
                <Button
                    Height="80"
                    Command="{Binding EquipmentPageCommand}"
                    Content="生产设备"
                    FontSize="30" />
                <Button
                    Height="80"
                    Command="{Binding UserPageCommand}"
                    Content="用户管理"
                    FontSize="30" />
                <Button
                    Height="80"
                    Command="{Binding SettingPageCommand}"
                    Content="系统设置"
                    FontSize="30" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
