<Window
    x:Class="CS_K_WPF.view.ExceptionWnd"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CS_K_WPF.view"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="ExceptionWnd"
    Width="800"
    Height="450"
    DataContext="{Binding Source={StaticResource ModelLocator}, Path=LocExceptionWndVM}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Title}" />
        <TextBlock Grid.Row="1" Text="{Binding MesForUser}" />
        <TextBlock Grid.Row="2" Text="{Binding MesForDev}" />


    </Grid>
</Window>
